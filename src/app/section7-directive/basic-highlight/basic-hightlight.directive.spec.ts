import { BasicHightlightDirective } from './basic-hightlight.directive';

describe('BasicHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
